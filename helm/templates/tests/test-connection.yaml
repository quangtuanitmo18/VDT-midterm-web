apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vdt-web.fullname" . }}-test-connection"
  labels:
    {{- include "vdt-web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "vdt-web.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
